# 2026-08-06 联网搜索策略与 Agent 控件执行记录

## 目标

把联网能力、用户本轮偏好、自动路由和实际执行结果拆开；确保内部 RAG 是质量下限，同时让外部证据可识别、可审计、可追溯。

## 执行结果

- 完成请求级 `auto | always | never` 契约和 System/Agent 联动。
- 完成基于时效、证据缺口与官方核验需求的自动路由。
- 完成正式引用、搜索参考和排除来源的分流。
- 完成 `[I#]` / `[W# 🌐]` 展示映射与流式来源协议。
- 完成按需深抓、逐跳 URL 校验和 SSRF 防护。
- 完成指标、8 条路由金标准和 P0 回归门。

## 验证

- 聚焦测试：92 passed。
- P0：230 个 unittest + 15 个 pytest + py_compile 全通过。
- 真实内部问答与真实强制联网/深抓问答均通过。
- 浏览器端到端验收通过，控制台无 error/warn。

## 暴露问题与反哺

第一轮真实测试发现“过去 7 天”空格解析和官网导航页误准入问题；两项均先补回归测试再修复。由此固定两条 Loop 规则：

1. 时间意图必须用自然语言变体 fixture 验证，不能只测理想输入。
2. “官方域名”只是候选排序信号，正式引用必须证明正文与主张匹配。

## 边界

- 不展示私有思维链，只展示真实执行进度。
- 不宣称已经获得稳定的总体 P95 改进；真实样本仍需积累。
- InfoQ 异常日期留给独立数据清洗 Stage。

详细证据：`claude-workspace/evidence/WEB_SEARCH_POLICY_AND_AGENT_TOGGLE_EVIDENCE_2026-08-06.md`。
