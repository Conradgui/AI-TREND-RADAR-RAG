# Stage 10 执行记录：浏览器用户流程 Gate

日期：2026-08-12
状态：通过

## 真实页面验证

运行地址：`http://127.0.0.1:8001`

- 时间、来源、分类筛选均已启用。
- 来源共 20 个动态选项，分类共 5 个动态选项。
- 近 7 天筛选：203 条，界面显示前 30 条并明确总数。
- 近 7 天 + OpenAI + 模型与技术突破：2 条；两条元数据均满足组合条件。
- `open ai` 被识别为 OpenAI，返回 246 个条目。
- 点击第一条进入 `#2026-08-05/ai-topic-radar/item/ATR-20260805-99E550`，详情包含摘要、产品判断、证据和原始来源链接。
- 页面标题与品牌均为 AI Trend Radar。

## 自动化测试与修复

首次运行 `tests/e2e/item-search.spec.ts`：4/5 通过。

失败路径：旧 `sd-v1` 书签进入 ATR 详情，跳转日报后再执行浏览器后退，详情被先前尚未完成的日报请求覆盖。

根因：`loadReport()` 的异步响应没有校验当前路由，过期响应仍可落屏。

最小修复：日报成功或失败响应写入页面前检查 `currentKey === key`；已导航到新路由时直接丢弃过期响应。

修复后：

- 失败用例定向复测：1/1 通过；
- 完整 item-search E2E：5/5 通过；
- `git diff --check`：通过。

## 部署验证

- 只重建 app 镜像并滚动替换 app 容器；没有删除 Neo4j 或任何数据卷。
- `/health` 返回 `status=ok`、`neo4j_connected=true`、`chromadb_chunks=3663`、`index_status=ready`、`provider=deepseek`。
- 部署后真实页面再次验证组合筛选，仍准确返回 2 条。

## 产品结论

用户报告的“时间点击无反应、来源与分类没有列表”在当前正式页面无法复现，且已有真实交互与 E2E 双重证据证明可用。因此本阶段没有重写筛选逻辑，只修复了同一用户流程中实际暴露的异步导航竞态。

## 独立质量监管结论

独立审查未发现 Web UI 代码级 P0，自动化 E2E 为 5/5。审查担心 E2E 使用模拟搜索索引，但本阶段已经在 Docker 正式服务上额外验证动态来源/分类列表、组合筛选和精确条目跳转，因此该风险已有更强的真实环境证据覆盖。搜索索引加载失败后的页面内重试按钮保留为后续体验优化，不阻塞当前 Gate。

## 残余风险

- 搜索结果当前最多展示 30 条，适合浏览但还不是分页式探索界面。
- `open ai` 的 246 条结果表明召回宽，排序质量仍应由任务级检索评估而不是本 UI Gate 判断。
- app 冷启动约 50–60 秒，期间 Docker health 为 `starting`；服务完成启动后健康。该问题属于运行时启动体验，不是本次筛选 Gate。
