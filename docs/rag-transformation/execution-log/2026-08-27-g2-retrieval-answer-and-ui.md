# G2 检索、回答与 UI 阶段记录

> 日期：2026-08-27
> 状态：执行中（Terra 独立审阅结论为 CONDITIONAL，尚未进入 G3）

## 目标与边界

- 冻结正式语料与活动索引，不新增语料、不重建 Neo4j/Chroma。
- 验证“问题路由 → 主体扩展 → 检索分层 → 层内排序 → 回答契约 → UI 深链”的真实用户链路。
- 只修复阻断 G2 的排序、认知负担、引用跳转和版本可见性问题；不扩展新产品功能。

## 运行基线

- 分支 / HEAD：`main` / `22191bf`（工作树包含此前尚未提交的合法开发改动）。
- 应用：`ai-trend-radar-rag-app-1`，健康。
- 图数据库：复用既有 `ai-trend-radar-rag-neo4j-1`，健康；未重建、未删除数据卷。
- 活动索引：`gen-20260821T074117-404548ad`，Chroma `4133` chunks。
- RAG 最新语料：`2026-08-21`；Web UI 可浏览日报已到 `2026-08-24`，存在三天可见性差异。

## 真实主路径证据

### “最近有什么热门趋势？”

- 接口：`POST /chat/stream`，`web_search_mode=never`。
- HTTP：`200`；总耗时 `8.80s`。
- 路由：`trend_discovery / trend_clusters`，来源 `product_catalog`。
- 检索：候选 `11`，正式采用内部引用 `6`；检索 `4.39s`，生成 `4.09s`。
- 执行：一次结构化检索 + 一次直接 composer；没有 Agent 工具循环。
- 证据：6/6 带稳定 ATR、本地条目深链和外部原始链接；Evidence Integrity 为 true。

### “Claude 最近有什么动态？”（修复前）

- 接口：`POST /chat/stream`，`web_search_mode=never`。
- HTTP：`200`；总耗时 `1.83s`。
- 路由：`trend_discovery / important_news`；Claude 确定性扩展到 Anthropic。
- 执行：确定性 important-news 路径；模型调用 `0`，总链路约 `1.79s`。
- 发现：关联主体 Anthropic 的公司新闻排在 Claude 产品动态之前；一次展示 18 条，且补充动态没有独立呈现。这违反“相关性先分主答案/补充/背景，再在层内排序”的产品合同。

## 浏览器验收（修复前）

- 首页、筛选器、Agent 面板均可加载；页面标题为 AI Trend Radar。
- 流式状态会先显示“问题已接收/思考中”，完成后渲染 Markdown。
- Claude 查询可完成，但复现上述主体层级与答案过长问题。
- 回答正文中的标题深链可以指向具体 ATR；底部引用卡片仍强制跳到日报首页，未使用 citation.local_url。
- 默认“自动联网”在内部已有证据时仍尝试外部搜索；未达到引用门槛后显示降级提示。该行为保留为后续策略验收项，不在本批扩大修改。

## 独立审阅

Terra 只读复核结论：`CONDITIONAL`。

主要理由：模块合同与聚焦测试证明链路基本存在，但合成校准集不能外推真实 Precision/Recall/F1；B 类确定性快速路径尚未统一 Answer Envelope；日报可浏览版本与 RAG 索引版本不一致。

## 本批停止条件

以下全部满足后才重新裁决 G2：

1. Claude 直接主体动态进入主答案，Anthropic 关联主体仅进入补充层；
2. 重要动态按主榜/补充/背景分区并有明确数量上限；
3. 确定性快速路径产出可验证 Answer Envelope，仍保持零额外模型调用；
4. 底部内部引用优先跳到具体 ATR；
5. 聚焦测试、全量后端测试、真实 API 与浏览器复验通过；
6. 10–20 条人工封存独立样本完成前，不发布 Precision/Recall/F1 数字。

## 账号切换 / 系统重启后的基础设施恢复

### 一键自检与安全自愈

- 新增公开入口 `./doctor.command`：默认检查并恢复已有 Docker 服务；`--check` 只读检查；`--open` 健康后打开 Web UI。
- `start.command` 统一委托给 Doctor，避免启动器和诊断脚本各维护一套 Docker/health 逻辑。
- 检查范围：`.env` 必填配置（密钥不输出）、Docker 引擎、Compose 配置、app/Neo4j 容器、`/health`、Neo4j 运行时连接、Chroma 非空索引、Provider 与检索模式。
- 修复边界：只启动或连接现有容器；仅在本机没有 app 镜像时执行首次构建；不会删除卷、清空数据库或自动重建索引。
- 本次系统重启后的真实故障为“容器运行，但应用到 Neo4j 的连接中断”；Doctor 调用 `/runtime/reconnect-databases` 后恢复成功。

### 冷启动根因与修复

- 修复前：应用冷启动约 9 分钟；首条 Claude 查询约 220.9 秒，暖机后相同链路约 3.67 秒。证据表明问题是启动资源竞争，而不是稳定态检索算法。
- 根因一：每次启动都重新扫描全部日报并重写约 10 MB 的 `search-index.json`。
- 根因二：每次启动都向健康 Neo4j 重放 11 条约束/索引 DDL。
- 修复：普通启动复用有效的 v2 搜索投影；显式 ingestion 仍负责重建。Neo4j 先做 readiness probe，仅在缺少必要索引时执行首次 schema provisioning。

### 修复后验证

- app 容器从启动到健康约 11 秒；FastAPI 初始化约 3.6 秒。
- Doctor 只读检查耗时约 1.82 秒。
- 首页推荐问题“最近有什么热门趋势？”：HTTP 200，9.10 秒，回答非空，6 条引用，无错误；联网搜索关闭。
- 聚焦回归：44/44 通过。
- 完整后端回归：929 tests + 40 subtests 全部通过。

## G2 主路径收敛复验

### 产品语义修正

- 普通问法“Claude 最近有什么动态？”不再被“重要新闻”的严格重要性门槛误伤：新鲜、直接属于 Claude 的研究动态可以进入主答案。
- 只有用户明确询问“重要/重大/值得关注”等动态时，才启用严格重要性门槛。
- Anthropic 公司级关联信息保留在“补充动态”，历史内容保留在“历史背景”；仍遵循“相关性先分层、再按新鲜度/重要性/证据质量层内排序”。
- Google Play、App Store、Product Hunt 的产品列表页被识别为导航页，只能作为搜索参考，不能充当近期新闻的正式证据。

### 真实 API 结果

- `web_search_mode=never`：HTTP 200，总耗时 `0.532s`。
- `web_search_mode=auto`：HTTP 200，总耗时 `2.620s`。
- 两种模式下主答案均非空，前两条均为 Claude 直接研究动态；正式答案包含 8 条内部引用、0 条外部正式引用。
- 自动联网模式中的 Google Play 结果仅进入 `search_references`，未进入正式结论；Evidence Integrity 为 true。

### 浏览器用户路径

- 报告、三个筛选器、Agent 和系统状态面板均能加载；控制台无 error/warn。
- 在 Agent 面板提交“Claude 最近有什么动态？”后约 5 秒内完成渲染；主答案、补充动态和历史背景分区正确。
- 8 条内部引用均使用具体 ATR 深链；点击首条后进入 `#2026-08-21/ai-topic-radar/item/ATR-20260821-C9EC37`，目标条目可见且无 404。
- 系统面板显示 Neo4j 已连接、ChromaDB `4133 chunks`、检索模式 `hybrid`。
- 一次自动化操作曾在折叠侧栏中直接修改隐藏筛选控件，随后点击自然未生效；这不是正常用户路径，因此没有据此修改产品逻辑。展开侧栏后按真实路径复验：时间/来源/分类从 `30/OpenAI/模型与技术突破` 一键恢复为 `all/空/空`，按钮自动禁用，结果恢复为 1966 个条目。

### 回归结果

- G2 聚焦回归：`98 passed, 3 subtests passed`。
- 全量后端回归：`929 passed, 40 subtests passed`。

## 独立 Stage Gate 裁决

Terra 只读复核结论：**CONDITIONAL**。

- G2 功能收敛子目标通过：路由、分层排序、Answer Envelope、ATR 深链、筛选与系统状态的真实主路径已验证。
- G2 检索质量证明尚未通过：10–20 条人工标注独立盲测集尚未封存，不能发布 Precision/Recall/F1。
- 数据可见性仍需显式处理：Web UI 可浏览到 `2026-08-24`，活动 RAG 索引冻结在 `2026-08-21`；用户必须能清楚知道 Agent 的证据截止日。
- 因此允许继续使用当前功能收集反馈，但暂不进入 G3 自动语料更新。下一次裁决只处理上述两个边界，不继续堆排序规则。

## 2026-08-28 Gate 阻断项收敛

### 数据可见性

- Agent 最终回答新增“📅 语料范围”，明确显示内部语料截止日期；浏览器实测当前显示 `内部语料截止 2026-08-21`。
- 该提示不伪装同步：Web UI 仍可浏览 8 月 24 日报告，但用户能看见 Agent 的实际证据边界。
- 聚焦回归：`30 passed`；Doctor 复核 app、Neo4j、Chroma 与 hybrid 链路全部正常。

### 独立检索集

- 发现原冻结的 20 条保留问题使用合成主体，只适合测试路由、指代和证据不足，不能用于真实语料检索质量评分；已停止错误复用。
- 隔离 Terra 审阅者在不读取实现、预测和评分文件的前提下，从冻结真实语料独立生成 12 条审阅草案：
  - 4 条具体条目导航；
  - 4 条近期趋势/重要动态；
  - 1 条关系比较、1 条时间线；
  - 2 条证据不足/需澄清。
- 草案路径：`docs/rag-transformation/evals/sealed/g2-real-corpus-retrieval-ai-review-2026-08-28.json`。
- 证据边界：这是隔离 AI 审阅草案，不是人类 Gold。Conrad 确认任务构成与标签方向之前，不运行正式评分、不发布 Precision/Recall/F1。

## 2026-08-28 模糊主体追问回归

- 用户真实输入 `open ai` 只有已知主体、没有任务目标；旧链路允许语义模型替用户猜任务，随后以“证据不足”拒答，错误地把对话歧义当成检索失败。
- 修复后，已知主体但缺少成功标准的请求在检索前确定性进入 `clarification_required`；不调用语义模型、不检索、不联网。
- 回答会确认识别到的主体，并给出“最近动态 / 产品与技术 / 对比关系 / 具体新闻”四个方向及可直接照抄的示例。未知指代仍沿用对象澄清，不混为同一种问题。
- 聚焦回归：`100 passed, 3 subtests passed`。真实 `/chat` 轨迹：`model_turns=0`、`tool_calls=0`、`citations=[]`；浏览器实测已显示引导式追问，未再出现证据不足拒答。
- 该输入已暴露给实现，作为产品回归样本保存，不纳入后续独立盲测评分，避免污染 G2 Precision/Recall/F1。

## 2026-08-28 独立检索集标签审计与评分接口

- 对 12 条隔离 AI 草案逐项核对 `digests/search-index.json`：共引用 21 个唯一 ATR，全部存在且能解析到具体 occurrence。
- 用户确认 Google/DeepMind 动态的标签修正：Dev.to 的 Gemini API 教程不属于公司/产品动态，移出主答案；Product Hunt 条目降为补充证据。
- “编码助手上下文”任务未找到第二种独立来源的合格条目，因此删除普通研究工具和 Gemini 教程，并移除语料无法支撑的“跨来源”要求；保留 claude-mem 与 Graphify 两种可验证产品做法。
- 新增 `rag/eval_g2_retrieval.py`：按 ATR 和回答层级分别评价导航、趋势、关系/时间线与证据不足，不计算语义无效的全局 F1。
- 正式评分硬门槛：数据必须标记为 `human_reviewed`；AI 草案只能使用显式诊断开关，不能生成发布成绩。
- TDD 证据：先出现 `ModuleNotFoundError`，实现后相关评估回归 `27 passed`。
- 当前状态：Conrad 已同意继续推进；任务集晋级为 `human_reviewed`，但保留“隔离 AI 提议 + 用户整体确认 + 两项显式人工修正”的来源边界，不夸大为全部人工逐条原创标注。

## 2026-08-28 外部检查表校准与计划合并

- 阅读用户指定的 RAG 面试题系列，重点核对 Chunking、Query Rewrite、多路召回、Graph 边界、幻觉控制、分层评估、动态更新和生产难点。
- 可吸收：业务数据 Gold、检索/生成分层诊断、词法/向量/图互补、低质量证据门控、稳定身份与内容指纹、影子版本和回滚。
- 不直接采纳：通用 Hit/MRR 阈值、默认 HyDE/多查询、默认 LLM Judge、通用固定 chunk 长度、消息队列预装，以及“向量检索绝对不能多跳”的简化结论。
- 效率调整：G2 剩余工作合并为一次“快照 → 分任务评分 → 一个首要瓶颈修复 → 回归/UI 复验 → Gate”；G3 合并为一次单日期影子更新与回滚包；G4 合并为一次干净 clone 部署包。
- 规范同步：更新 CURRENT_CONTROL、G0–G4 执行计划、目标架构和离线评估合同；未建立平行 roadmap 或新框架。

## 2026-08-28 真实小样诊断与 Agentic RAG 校准

### 小样先行

- 正式 12 条评估前先跑 4 条阻断性小样；避免在错误架构上消耗完整模型预算。
- `Graphify` 功能描述与指定日期 `open-webui` 已稳定进入确定性导航，分别约 `16ms` 与 `29ms`，模型调用为 `0`。
- Claude Code 自动模式记录最初未命中；根因是词法导航没有识别“自动模式 / 人工审批 / 危险命令”这一组辨识指纹。补充受控概念后，真实服务约 `89ms` 命中 `ATR-20260811-EF1380`，模型调用为 `0`。
- `Graphify vs claude-mem` 最初丢失后者；根因一是全小写连字符产品名未被 Query 保护，根因二是跨日报重复 occurrence 在小候选池内造成候选饥饿。修复为通用的小写 slug 保护和 `owner/repository` 仓库名别名，不以扩大 Top K 掩盖问题。
- 继续复测发现精确标题会越过 Route Contract，把比较题劫持成单条导航；已新增硬边界：存在 Route Contract 时，只有 `item_navigation` 可进入 Navigator。
- 聚焦回归从 `50/50` 增至 `54/54`，全部通过。

### Agentic RAG 架构边界

- 确定性 Workflow：精确条目导航、首页热门趋势等高频且成功标准明确的任务，目标是低延迟、零模型或单次生成。
- 受约束 Agentic RAG：比较、关系、核验和深度研究，允许有限规划、按任务选工具、结果驱动的有限调整，但必须受工具白名单、预算、停止条件和证据合同约束。
- 不采用“所有任务都自由 ReAct”或“为了 Agentic 而多 Agent”；这会放大延迟、成本、不可复现性和恢复难度。
- 面试与证据映射已固化到 `evidence/2026-08-28-agentic-rag-interview-readiness.md`。

### 当前阻断

- 本地代码、确定性检索和 54 条聚焦回归均已完成。
- 比较题真实复测与完整 12 条快照会将内部语料片段发送给外部 DeepSeek；当前执行审批要求再次明确到该目的地，因此未绕过审批继续调用。
- 在获得明确授权并完成快照前，G2 仍为 `CONDITIONAL`，不得发布最终检索成绩。

## 2026-09-01 聚焦检索回归：混合候选截断修复

### 根因与修复边界

- 真实问题“按时间线梳理与 OpenAI 潜在上市相关的两条直接报道”已由词法索引找到 `ATR-20260820-6EFF79`，但混合检索 RRF 的短候选池在融合后将其截断；这不是 Neo4j 连通性、模型推理或最终时间线排序的故障。
- 修复仅作用于 **Route Contract 已判定为直报时间线、且不要求图谱** 的路径：将本地词法已经标注为 `entity_event`（或精确匹配）的有限候选并入混合结果，再按“事件直接性 → 查询匹配 → 新鲜度”进行回答前预算排序。
- 不改变普通问题的混合检索、Graph RAG 关系查询，也不增加模型调用或外部搜索。

### 真实运行时复验

- app 与既有 Neo4j 均健康；活动索引为 `gen-20260831T083806-39f9d2a6`，Chroma `4286` chunks。
- 同一真实时间线问题：`6.75s` 总耗时、模型调用 `0`、工具调用 `0`；返回 `ATR-20260812-0E70FB` 与 `ATR-20260820-6EFF79` 两条直接报道，并保留各自 ATR 深链。
- 回归：`101 passed, 3 subtests passed`（时间线、Gateway、路由策略、查询理解与 G2 评分器相关范围）。

### 聚焦 G2 证据

- 基于已审阅三条聚焦集和显式稳定内容等价规则，v9 快照 / v10 评分结果为 `3/3`：近期趋势、关系比较、时间线各 `1/1`。
- 评分器同时修正了诊断字段：若新日报 occurrence 的 `content_id` 与审阅 Gold 的稳定内容相同，不再被错误列为“未判定”。原始 occurrence 仍保留为用户可跳转的引用身份。
- **边界**：这只是本轮三条真实主路径的回归证明，不是 12 条独立盲测完成，也不是全局 Precision、Recall 或 F1；G2 的整体 Gate 仍保持 `CONDITIONAL`。

## 2026-09-01 截止日期检索空结果修复

### 问题与根因

- 真实请求“截至 8 月 21 日，过去一周 OpenAI 有哪些值得关注的业务或产品动态？”已被 Query Frame 正确解析为 `2026-08-15` 至 `2026-08-21`，但趋势检索返回空结果。
- 根因位于本地词法索引的 metadata 兼容层：它支持 `$in`，却把 Chroma 形状的 `$gte` / `$lte` 日期条件当作完全相等比较，因此把所有日期记录静默排除。
- 这不是 Neo4j、向量数据、模型推理或“没有相关新闻”的问题。

### 修复与验证

- `metadata_matches_filter` 现支持 ISO-8601 日期的包容范围比较，并新增复合 `$and` + `$gte` / `$lte` 的回归测试。
- 相关回归：`145 passed, 3 subtests passed`。
- 应用容器单独重建；既有 Neo4j 容器和数据卷未重建。健康检查：Neo4j、混合检索和活动索引均为 `ready`。
- 同一真实请求恢复 `22` 个候选，返回的展示引用日期均处于 `2026-08-15` 至 `2026-08-21`；运行约 `0.3s`，零模型调用、零联网调用。

### G2 评分边界

- 已产生的 12 条完整快照只能用于诊断：冻结 Gold 的语料截止日为 `2026-08-21`，而当前活动索引可见至 `2026-08-24`。两者边界不一致，不能把该次 `7/12` 结果发布为正式质量成绩。
- 快照执行器已改为逐条 checkpoint、可 `--resume`、错误条目可重试，避免限流或中断导致整批重跑。
- 下一次正式 G2 必须二选一：在与 Gold 相同截止日的活动 generation 上运行，或重新冻结独立、带来源边界的当前语料 Gold；在此之前 G2 继续保持 `CONDITIONAL`。

## 2026-09-01 冻结 generation 正式 G2 结果与下一瓶颈

### 可比运行

- 评估服务固定读取已验证的 `gen-20260821T074117-404548ad`，其 manifest 为 `4133` 条、最新日期 `2026-08-21`；用户主服务继续使用 `gen-20260831T083806-39f9d2a6`，没有临时切换主指针。
- 12 条冻结 Gold 快照逐条完成，`request_failures=0`；结果文件：[快照](../evals/g2-real-corpus-retrieval-run-2026-09-01-frozen-20260821.json) 和 [评分](../evals/g2-real-corpus-retrieval-score-2026-09-01-frozen-20260821.json)。

### 分任务结果

| 任务 | 结果 | 解释 |
|---|---:|---|
| 具体条目导航 | 4/4 | 稳定 ATR 身份与直达能力通过 |
| 证据不足/澄清 | 2/2 | 没有证据时不编造，歧义时先追问 |
| 关系比较 | 1/1 | 关系题可按证据边界回答 |
| 近期趋势 | 1/4 | 主要暴露排序与 Gold 内容版本对齐问题，另发现“截止日 + 最近”窗口过宽 |
| 时间线 | 0/1 | 旧 generation 中缺少 Gold 对应的后期 occurrence，不能据此修改已通过的时间线候选修复 |
| 合计 | 8/12 | 仅作为冻结 generation 上的分任务诊断，不合成全局 Precision/Recall/F1 |

### 后续最小动作

- `截至日期 + 最近` 现在会保留现有“最近 14 天”语义；“过去一周”仍优先使用 7 天。已新增查询理解回归，避免旧内容挤入近期趋势候选池。
- 不针对旧 generation 缺失的数据编写特殊排序规则。下一次正式 G2 应在与当前 Gold 内容 revision 对齐的 generation 上重跑，或重新冻结一份对应当前 generation 的独立 Gold。
- 应用主服务的日期筛选修复已加载并完成真实接口验证；最近窗口组合修复已通过源码回归，待下一次正常应用重建后进入主服务。此次重建只需 app，不需重建 Neo4j。

## 2026-09-01 G2 最终收口：PASS

### 最小修复与验证

- 复核发现上一轮 `11/12` 的唯一失败为 `g2-real-006`：运行中的 app 仍使用旧镜像，旧索引中的 Anthropic 官方条目被误判为 `developer_content/documentation_or_tutorial`，未进入重要动态主榜；不是 Neo4j、ChromaDB 或请求超时。
- 最小修复：收紧开发者内容识别，避免把 “model developers” 和 “agent interaction” 等偶然措辞误判为开发者文档；旧索引事件契约允许从开发者/文档占位升级为真实新闻或研究事件；EU AI Act 合规变化、multiagent 系统性失败等高影响信号进入主榜排序。
- TDD 验证：事件分类与实体/重要性排序聚焦回归 `24 passed, 4 subtests passed`。
- 仅重建 app 容器加载已验证代码；Neo4j 容器 `ai-trend-radar-rag-neo4j-1` 与数据卷未重建。健康检查：`configured=true`、`neo4j_connected=true`、ChromaDB `4286` 条、generation `gen-20260831T083806-39f9d2a6`。

### 最终真实语料验收

- 运行快照：[G2 12 条最终快照](../evals/g2-real-corpus-retrieval-run-2026-09-01-active-20260831-v3.json)。服务端按 4 条一批、遵守 10 次/60 秒限流；12/12 请求成功，`request_failures=0`。
- 正式评分：[G2 最终评分](../evals/g2-real-corpus-retrieval-score-2026-09-01-active-20260831-v3.json)。12/12 通过：`recent_trend 4/4`、`item_navigation 4/4`、`evidence_insufficiency 2/2`、`relation_comparison 1/1`、`timeline 1/1`。
- 评分策略仍为 `task_specific_no_fake_global_f1`：不把不同任务强行合成为一个全局 Precision/Recall/F1；这是 G2 分任务验收成绩，不是对全部未来语料的泛化保证。

### Gate 决定

- **G2：PASS，正式收口。** 路由、分层排序、证据边界、ATR 深链、时间线、关系比较和证据不足处理已在本批真实任务中闭环验证。
- **G3：未开始/未放行。** 自动抓取、规范化新日期、双库一致性、影子 generation、回滚与 GitHub Runner 仍按下一阶段执行；G2 通过不等于自动更新已开启。
