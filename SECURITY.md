# 安全说明

## 支持范围

本项目默认面向单台本地电脑使用。不要把本地 8001 端口、Neo4j 数据库或 `.env` 文件直接暴露到公网。

## 密钥与数据

- `.env` 保存 Provider 和可选联网搜索密钥，已被 Git 忽略；不要提交、截图或粘贴其内容。
- 默认 Docker 配置只公开浏览器需要的 8001 端口，不公开 Neo4j 端口。
- `RAG_API_KEY` 仅适用于需要对外提供 API 的高级部署。设置它前，请为 API 客户端实现 `X-API-Key` 请求头；本地浏览器仪表盘不会保存该密钥。

## 报告漏洞

请不要在公开 Issue 中附上可利用步骤、真实密钥或私有数据。请通过仓库维护者的 GitHub 联系方式私下报告，并说明受影响版本、复现条件和潜在影响。
